# Proof Receipt — P05

## Identity
- **Packet:** P05
- **Title:** Mode-specific bounded context compilers (journal / reflection / adventure-scene / snapshot)
- **Base SHA:** `75ed3a54038bb45aca07b830b47cdc0741580492`
- **Branch:** `lane/provider-p05-p10-p12`
- **Executor:** provider lane (Claude Opus 5.5)
- **Risk / proof class:** H privacy/budget

## Changed-file envelope
- `src/cartographer/modes/budget.ts` — `MODE_CONTEXT_BUDGET` (extends, does not replace, legacy `CONTEXT_BUDGET`), `takeWithinBudget`, `serializeModeContext`
- `src/cartographer/modes/doNotReassert.ts` — RF04 rejected interpretations, bounded
- `src/cartographer/modes/journal.ts` — `compileJournalModeContext`
- `src/cartographer/modes/reflection.ts` — `compileReflectionModeContext`, `compileReflectionHistory`
- `src/cartographer/modes/adventure.ts` — `compileAdventureSceneModeContext`
- `src/cartographer/modes/snapshot.ts` — `compileSnapshotModeContext`
- `src/cartographer/modes/index.ts`
- `tsconfig.worker.json` — **outside owned paths**: adds `"exclude": ["src/cartographer/modes"]`. See reviewer note.
- `tests/cartographer/mode-contexts.test.ts` (15 tests)

No hot-zone file, schema, migration, Worker code, dependency or `main` change.

## Acceptance
- Each compiler is a pure `(state, task) -> context` function with explicit item + char caps: PASS.
- Built only from structurally eligible records: journal via `selectProviderEligibleJournalEntries`; gaps/seeds/evidence/insights/contradictions/reflections/observations via `createV2ProvenanceVisibility`; memories via `eligibleAdventureMemories` -> N02 retrieval -> N03 `selectMemoriesWithinBudget`: PASS.
- Each returns the allow-list of exactly the IDs included (`allowedIds` / `allowedSourceIds` / `allowedProvenanceIds`), and tests feed them straight into `parseReflectionProposalForContext`, `parseAdventureSceneProposalForContext`, `parseSnapshotProposalForContext`, rejecting any ID that was not sent: PASS.
- Reflection and Snapshot contexts include RF04 `doNotReassert`; those IDs are deliberately NOT in the citable allow-list (test: citing `r_rej`/`in_rej` throws): PASS.
- Per-mode PRIVATE/retracted canary: a fixture where every ineligible record (PRIVATE/retracted journal, private-dimension/retracted evidence, derived insights/contradictions, PRIVATE and privacy-flagged reflections, PRIVATE-sourced gap/seed/run/action, private/mixed/flagged memories) carries `CANARY` in both ID and text; serialized context never contains it, for all four modes: PASS.
- Ineligible task target (PRIVATE/retracted current entry, no eligible Reflection sources, run with PRIVATE seed provenance) throws so the caller degrades to P08/P09/P10 local fallbacks: PASS.
- Budget max-size tests with 200 journal entries / 50 sources / 40 actions + 30 memories / 100 evidence: PASS.
- Deterministic output (identical serialization on repeated compile): PASS.
- Adventure context contains no journal text (asserted per entry): PASS.

## Validation executed (local macOS)
- Focused: `tests/cartographer/mode-contexts.test.ts` **15/15**
- `npm run typecheck` (app + node + worker projects): clean
- `npx vitest run`: **1047/1047 across 117 files** (1020 baseline + 27 new in this lane: P05 15, P10 6, P12 6)

## Validation not executed
- Character caps are conservative placeholders; not measured against the selected Workers AI model / free-plan budget (plan 14.3 says to measure — P06 / provider evaluation).
- No caller wiring: nothing in the app/worker invokes these compilers yet (P06 prompts + integration).
- Browser suite / build not run for this packet (no UI or bundle-facing change).

## Privacy / cost / persistence impact
Reduces what may leave the device: all four modes are structural allow-lists. No persisted schema, migration, remote service, dependency or spend.

## Reviewer notes
- `tsconfig.worker.json` includes all of `src/cartographer`, and the compilers necessarily import app domain modules (`persistence/retirement`, `journal/domain`, `adventure/memory/*`, `reflection/antiRepeat`). Rather than widening the Worker program, `src/cartographer/modes` is excluded from it: compilers run client-side and the Worker receives an already-compiled context. Consequence: the Worker cannot import `modes/providerPolicy.ts` (P12) either. If worker-side quota enforcement needs it, move that one file out of `modes/`.
- Journal mode sends the current entry (≤4000 chars) plus ≤3 recent eligible entries (≤280 chars each). Whether recent entries should travel at all is a product decision for P06.

## Verdict
READY_FOR_REVIEW

## Next packets unblocked
P06 (mode prompts), P12 wiring, Q06

## Integration review
Accepted tsconfig.worker.json exclude of src/cartographer/modes (app-side compilers). Not yet wired into a provider route (P06/Q06). MERGE_READY for the pure modules.
