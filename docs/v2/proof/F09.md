# F09 — Shared-file hot-zone ownership

## Packet

- packet: `F09`
- branch: `docs/v2-execution-contracts`
- artifact: `docs/v2/OWNERSHIP_MATRIX.md`

## Result

The master plan's shared-file hot zone and lane ownership are compiled into one implementation-facing matrix.

It explicitly protects:

- `App.tsx`;
- shared game types/engine;
- persistence schema/migrations;
- provider root schema and Worker;
- package/dependency surface;
- repository authority/state files;
- central asset manifests.

It also freezes lane-owned directories and the authority-transfer procedure required when a lane needs a shared interface changed.

No runtime source, schema, dependency or product behavior changed.

## Verdict

**MERGE_READY** after documentation diff review.

