# Proof Receipt — Q02

## Identity
- **Packet:** Q02
- **Title:** TypeScript gate: real typecheck + CI step
- **Base SHA:** `0f828e2`
- **Executor / Reviewer:** Claude Opus 5.5 (integration owner)
- **Risk / proof class:** C gate

## Finding
`npx tsc --noEmit -p .` — used as "tsc clean" evidence in earlier receipts (including this cycle's P02/S00/K03/C06/C05/P00/J06/J08 receipts) — is vacuous: root `tsconfig.json` has `files: []` and only project references. CI never typechecked (vite build strips types). At `b262901` the real projects had 13 (app) + 5 (worker) errors; at `0f828e2`, 10 + 7.

Earlier "tsc clean" lines in receipts should be read as UNVERIFIED for type safety; unit/build/browser evidence in them stands.

## Changed-file envelope
- `package.json` — hot zone: add `typecheck` script (no dependency change).
- `.github/workflows/ci.yml`: add Typecheck step before tests.
- `tsconfig.worker.json`: include the schema modules `src/game/types.ts` transitively imports.
- Test-only type fixes: `tests/adventure/seeds.test.ts`, `tests/atlas/eligibility.test.ts`, `tests/cartographer/proposal-repair.test.ts`, `tests/game/onboarding.test.ts`, `tests/journal/links.test.ts`, `tests/knowledge/relevance.test.ts`, `tests/knowledge/seedRequest.test.ts`, `tests/persistence/v2-transfer-validation.test.ts`.

## Semantic fixture bugs exposed (repaired, not weakened)
- K03 test used contradiction status `'reconciled'` (does not exist; enum is open/resolved). Case passed vacuously; now uses `'resolved'`.
- S01 fixture used `learningTarget: 'fun'` (invalid); now `'none'`.
- K07 fixture used territory status `'available'` (invalid); now `'exploring'`. Assertions unchanged and passing.
- Remaining edits are casts on deliberately-tampered inputs.

## Validation executed
- `npm run typecheck`: 0 errors (app, node, worker)
- unit: 763/763 across 89 files

## Reviewer verdict
**MERGE_READY**
