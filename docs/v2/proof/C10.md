# Proof Receipt — C10

## Identity
- **Packet:** C10
- **Title:** Minimal statuses and resolution order
- **Base SHA:** `4efcf062ea47cab19a92051532cfaa62d9692d14`
- **Final implementation SHA:** `8a679ee`
- **Branch:** `v2/combat-c07-c12`
- **Pull request:** draft PR for this branch
- **Executor:** Claude Opus 5.5 (combat lane)
- **Reviewer:** pending
- **Risk / proof class:** H (order/idempotency)

## Changed-file envelope
- `src/combat/statuses.ts` (new: `STATUS_RESOLUTION_ORDER`, `DEFAULT_STATUS_DURATION`, `applyStatus`, `removeStatus`, `tickStatuses`, `hasStatus`, `declareGuard`)
- `tests/combat/statuses.test.ts`

Committed before C08/C09 because both use these primitives. The plan row says C10 depends only on C01.

## Acceptance matrix
| Criterion | Evidence | State |
|---|---|---|
| only the six MVP statuses; unknown ids rejected | order + validation tests | PASS |
| frozen order: staggered, charging, guarded, protected-target, exposed, pacifiable; stored order does not depend on application order | "stores statuses in frozen order" | PASS |
| idempotent apply (no stacking, refresh to max) and remove | idempotency tests | PASS |
| durations 1-2 only; deterministic tick at end of enemy phase | duration/tick tests | PASS |
| no generic poison/burn families | unknown-id rejection | PASS |

## Conservative interpretations (review)
- Durations: guarded 1, staggered 1, protected-target 1, exposed 2, charging 2, pacifiable 2. Charging and exposed need 2 to survive the end-of-enemy-phase tick into the next phase.
- A GUARD declaration is modelled as the `guarded` status. Perfect-guard timing is injected when the enemy phase resolves.

## Validation executed (local macOS, head `4bde84e` before receipts)
- `npx tsc --noEmit -p .`: clean
- `npx vitest run`: **642/642 across 74 files** (baseline 506/506 across 69)
- Focused: `tests/combat/statuses.test.ts` 10/10

## Validation not executed
- Build, browser suite and CI were not run by the executor (UNVERIFIED until PR CI).

## Privacy / cost / persistence impact
Personal-data eligibility: none. Provider context: none. Persisted schema: none. Dependencies: none. Spend: none.

## Reviewer verdict
Executor: READY_FOR_REVIEW (not self-certified; H-class).

## Next packets unblocked
C08, C09 (landed on this branch), C11
