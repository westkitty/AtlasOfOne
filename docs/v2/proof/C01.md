# Proof Receipt — C01

## Identity

- **Packet:** C01
- **Title:** Implement CombatDefinition/CombatState and lifecycle reducer
- **Base gate:** integration `6802a34318740633649b977f52f581a66dfa0772`
- **Implementation head before receipts:** `f58e20fd1b0843219dc3d6ebb3db1b10d2d21d47`
- **Combined Wave-2 branch:** `feat/v2-wave2-foundations-core`
- **Risk / proof class:** R2 deterministic game-authority foundation

## Changed-file envelope

C01 owns only:

- `src/combat/types.ts`
- `src/combat/engine.ts`
- `tests/combat/engine.test.ts`

No persistence, shared GameEvent, provider, UI, assets, dependencies, rewards application, attack/guard/technique/ACT/leave mechanics, or deployment wiring.

## Frozen behavior implemented

- exact C00 verb/objective/gimmick/intent/status registries as types;
- deterministic `CombatDefinition` and `CombatState`;
- exactly one player and at least one enemy;
- encounter-local player baseline locked to **100 HP / 2 Technique charges**;
- fixed XP reward must be explicit and integer;
- player acts first unless an explicit authored `ambush` reason opens enemy phase;
- deterministic state construction;
- lifecycle reducer only: player phase -> enemy phase -> next round, or one terminal outcome;
- conflicting post-resolution outcome is refused.

## Bounded review repairs before CI

Static review found and repaired two authority leaks:

1. enemy opening phase originally lacked the required explicit ambush reason;
2. player max HP was frozen at 100, but authored `startingHp` and `techniqueCharges` could bypass the C00 100/2 baseline.

Focused tests now reject both.

## Acceptance matrix

| Criterion | State before PR CI |
|---|---|
| deterministic initial state | focused test present |
| no random initiative | focused test present |
| only telegraphed authored ambush may open enemy phase | focused negative test present |
| C00 100 HP / 2 charges cannot be silently retuned | focused negative tests present |
| invalid lifecycle transition fails closed | focused test present |
| resolution is terminal/idempotent only for identical outcome | focused test present |
| no model/mechanics/persistence authority introduced | scope-inspected |
| full existing browser behavior preserved | **requires exact-head PR CI** |

## Explicit non-goals

C02-C10 own combat verbs/formulas/objectives/gimmicks/status resolution. C11 owns persistence. C12 owns model-authority mutation proof. C13+ own UI/accessibility/tuning.

## Reviewer verdict

**MERGE_READY only if the receipt-bearing combined PR head passes the full Atlas validation suite.**
