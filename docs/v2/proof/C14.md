# Proof Receipt — C14

## Identity
- **Packet:** C14
- **Title:** Timed-input accessibility fallback / reduced motion
- **Base SHA:** `45b57215db4a2dd0e1b9d6da16716d2c82e40aa9`
- **Branch:** `lane/v2-combat-presentation-content`
- **Executor:** Claude Opus 5.5
- **Risk / proof class:** H (combat input authority)

## Changed-file envelope
- `src/combat/timing.ts` — `resolveTimedSuccess`, `timingPromptOffered`, `TIMING_WINDOW_MS = 300`
- `tests/combat/timing.test.ts`
- `docs/v2/proof/C14.md`

## Acceptance
- Keyboard, touch, declined, reduced-motion, missing sample, NaN/Infinity offset, and out-of-window samples all map to `timedSuccess = false` (base action); there is no failure state: PASS (13 cases).
- Generous ±300 ms window for opted-in timed pointer only; reduced motion never offers a prompt and never judges timing: PASS.
- ATTACK without timing = 18; GUARD without timing = ceil(50%) for every raw 0-22; timing only improves: PASS.
- End-to-end via `playRound`: missing / false / keyboard-resolved timing give deep-equal states: PASS.
- All 30 CT04 encounters are won with zero timed inputs; adding perfect timing never changes outcome, never adds turns, never lowers player HP: PASS.
- No clocks read; tests inject samples: PASS.

## Validation executed
- Focused: `tests/combat/timing.test.ts` 21/21.
- Full lane run: 876/876 unit across 93 files.

## Validation not executed
- Real browser keyboard/touch/reduced-motion proof: UNVERIFIED. The timing prompt UI isn't wired into the App (integration owner, I02). The CombatPanel has no timing prompt, so its intents never carry `timedSuccess`, which is the base path.

## Privacy / cost / persistence impact
None.

## Verdict
**READY_FOR_REVIEW** (browser proof deferred to integration).

## Next packets
I02, C17.
