# Proof Receipt — A08

## Identity
- **Packet:** A08
- **Title:** Persist/reload active adventure mid-beat
- **Base SHA:** `45b5721`
- **Executor / Reviewer:** Claude Opus 5.5 (integration owner; persistence hot zone)
- **Risk / proof class:** H persistence

## Changed-file envelope
- `src/combat/persistence.ts` (new: durable combat schema, record creation, fail-closed resume)
- `src/persistence/schema-v2.ts` — hot zone: additive `activeCombat` nullable default null. No version bump (additive, defaulted).
- `src/game/types.ts` — hot zone: `CampaignState.activeCombat`.
- `src/game/engine.ts` — hot zone: initial campaign sets `activeCombat: null`.
- `tests/persistence/v1-to-v2-migration.test.ts` (projection strips the new scalar; asserts null)
- `tests/persistence/combat-adventure-persistence.test.ts` (new, 6 tests: 5 C11, 1 A08)

## Acceptance
- Active run + started seed + actions + unreflected observations round-trip through export/import unchanged: PASS.
- Restored run continues deterministically from the saved beat (same next state as never-saved run): PASS.
- Evidence unchanged by adventure persistence: PASS.
- IndexedDB uses the same `migrateCampaign` boundary as import (src/persistence/db.ts), so transfer proof covers the parse path; a dedicated browser reload of an active adventure is deferred to I01/I08 once App starts runs.

## Validation executed (local, integration candidate)
- unit: **763/763 across 89 files**
- build: PASS, precache 120 entries (628.11 KiB)
- browser: **155/155 across 19 files**
- Note: `npx tsc --noEmit -p .` is vacuous (root config has `files: []`); real typecheck fixed and gated in Q02.

## Validation not executed
- Browser IndexedDB reload of active combat: pending I02 App wiring.

## Privacy / cost / persistence impact
Persisted schema: additive field (C11). No personal data, provider context, or spend.

## Reviewer verdict
**MERGE_READY**

## Next packets unblocked
I02, I08, Q03
