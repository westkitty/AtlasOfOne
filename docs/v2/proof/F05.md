# F05 — Re-run exact pre-change baseline

## Base

Integration branch CI head: `24631bf8dddf593e8c43e0a3582342b31d460e6f`

GitHub Actions run: `35821752924` — **Atlas validation #51**

## Validation actually run

- dependency installation — passed
- `npm test` / Vitest unit suite — **38 files, 304 tests passed**
- `npm run build` — production Worker/client build passed
- PWA generation — **120 precache entries**
- `npm run test:browser` / real Chrome browser suite — **15 files, 143 tests passed**

The build retained the existing non-fatal chunk-size warning; no build failure occurred.

## Protected baseline evidence

The browser suite proved the existing Worldwalker and campaign baseline before v2 runtime mutation, including:

- movement and sanctuary interaction;
- persistence/reload;
- export/delete/import;
- PRIVATE filtering;
- STOP / PASS / SERIOUS / sass controls;
- Boss and Mystery deterministic encounters;
- concurrency guards;
- provider degradation;
- onboarding continuity;
- existing voice/TTS behavior as the **pre-removal baseline**.

That final item is intentionally historical proof, not a requirement to preserve TTS.

## Environment

- runner: GitHub Actions `ubuntu-latest`
- local MacBook worktree remained untouched because the remote bridge was unavailable.

## Verdict

**PASS.**

The v2 runtime lanes are no longer blocked on baseline proof. V00 inventory may close and V01 STT-only implementation may proceed to independent review.
