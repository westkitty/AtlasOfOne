# Proof Receipt — J04

## Identity

- **Packet:** J04
- **Title:** Wire STT transcript into the editable Journal composer
- **Base SHA:** `45cf468b7534859ef8a6977d59d1a5be29849125`
- **Branch:** `feat/v2-journal-stt`
- **Executor:** GPT-5.6 Sol / GitHub connector
- **Reviewer:** browser/async lifecycle review + GitHub Actions #153
- **Risk / proof class:** R2 browser microphone/STT integration

## Changed-file envelope

- `src/App.tsx`
- `src/journal/JournalComposer.tsx`
- `src/styles.css`
- `tests/browser/journal-stt.test.ts`
- `docs/v2/proof/J04.md`

No provider schema, shared GameEvent, progression rule, evidence/insight conversion, persistence schema, dependency, deployment or `main` mutation.

## Behavior contract

- dictation starts only from an explicit Journal button;
- the same existing microphone/capture/transcription lifecycle is reused;
- Journal dictation bypasses spoken agency-command interpretation and becomes editable draft text only;
- existing draft text is preserved and transcript is appended;
- successful dictation marks the unsaved draft origin as `speech-to-text`;
- editing after transcription remains allowed;
- Save remains explicit and is disabled while microphone permission/listening/transcription is busy;
- no Journal dictation automatically saves, creates a Turn/Evidence/Insight, awards XP, calls `/api/turn`, or reopens the microphone;
- closing Journal invalidates/cancels in-flight capture/transcription so stale completion cannot land in a hidden draft;
- if capture is unavailable/offline, typing remains complete.

## Validation executed

GitHub Actions **Atlas validation #153**, run `36030109102`, head `cae0292d47eaea212ee26955c7b4d64719fb4ef9`:

- `tests/browser/journal-stt.test.ts`: **4/4 passed**
- unit: **53 files / 372 tests passed**
- production/PWA build: PASS
- PWA precache: **120 entries (606.82 KiB)**
- browser: **17 files / 143 tests passed**

The browser proof demonstrates:

1. transcript enters the same blank Journal textarea;
2. no durable Journal write occurs before Save;
3. no `/api/turn`, XP or TurnRecord side effect occurs;
4. transcript remains editable;
5. explicit Save records `inputMode:'speech-to-text'`;
6. delayed permission acquisition is invalidated if Journal closes;
7. closing during delayed transcription prevents stale transcript application.

### Bounded browser-fixture repair

An intermediate J04 test-only head failed because the new permission-race test reopened the Journal to verify draft preservation and left that overlay open. The next test therefore could not find the map-level `open-journal` control.

That failure never reached the product behavior under test. Repair added explicit test cleanup:

```ts
await page.click('[data-testid="journal-close"]');
```

No J04 production code changed in that repair. Final CI #153 is green.

## Validation not executed

- physical mobile microphone
- Safari/iOS microphone capture
- production deployment

## Reviewer verdict

**MERGE_READY after CI on this proof-only receipt head remains green.**
