# Proof Receipt — A09

## Identity
- **Packet:** A09
- **Title:** Pure-fun seed path with learningTarget none
- **Base SHA:** `4efcf062ea47cab19a92051532cfaa62d9692d14`
- **Branch:** `v2/knowledge-adventure-k07-a09`
- **Pull request:** draft to `integration/atlas-v2-journal-adventure-combat` (see PR body)
- **Executor:** Knowledge -> Adventure lane (Claude Opus 5.5)
- **Risk / proof class:** S

## Changed-file envelope
- `src/adventure/seeds.ts` (`createPureFunAdventureSeed`)
- `src/adventure/beats.ts` (plan omits reflection)
- A09 cases in `tests/adventure/{seeds,run,beats,outcomes}.test.ts`
- `docs/v2/proof/A09.md` (this receipt)

No hot-zone file, `GameEvent` widening, persistence schema, migration, Worker, dependency, deployment, or `main` mutation.

## Acceptance
- Pure-fun seed: kind pure-fun, no source gaps, learningTarget none: PASS
- Eligible with zero KnowledgeGaps; a gap-free seed claiming reflection-eligible is ineligible: PASS
- No-reflection-required: a pure-fun run completes from choice-consequence; the reflection beat is never reachable: PASS
- Completion outcome reflectionOffered false: PASS
- K07 refuses pure-fun on the gap path: PASS

Note: implemented inside the A00/A01 commits (shared files); no separate code commit.

## Validation executed (local macOS, branch `v2/knowledge-adventure-k07-a09`)
- A09 cases: 4 in seeds.test.ts, 1 in run.test.ts, 2 in beats.test.ts, 1 in outcomes.test.ts (all passing)
- `npx tsc --noEmit -p .`: clean (exit 0)
- `npx vitest run` full unit suite: **554/554 across 75 files** (baseline 506/69 at `4efcf06` + 48 new tests in 6 new files; exact sum)
- First full run showed one timing failure in `tests/cartographer/context.test.ts` ("stays under an absolute ceiling even at 600 turns"), outside this lane; it passed 12/12 in isolation and the immediate full re-run was 554/554. Recorded as a load-sensitive flake, not caused by this change.

## Validation not executed
- `npm run build`, PWA precache count, browser suite: not run locally; left to PR CI.
- Physical device / live provider: not applicable (no provider path).

## Privacy / cost / persistence impact
Provider context: none. Network: none. Dependency: none. Spend: none. Persisted schema: none (no edit to `src/persistence/*`); any new durable fields or event wiring are deferred to the integration owner (A08). Fixtures are synthetic.

## Reviewer verdict
**MERGE_READY (after integration review)** (executor self-check PASS; semantic review by integration owner pending on the draft PR).

## Next packets unblocked
A06.

## Integration review + repair (Claude Opus 5.5, integration owner)
Independent adversarial review (feature-dev:code-reviewer, read-only against PR head): **MERGE_READY**.

Combined integration after repair: 757/757 unit across 88 files, tsc clean.
