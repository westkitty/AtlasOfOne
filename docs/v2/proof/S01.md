# Proof Receipt — S01

## Identity
- **Packet:** S01
- **Title:** Deterministic Snapshot eligibility / request path
- **Base SHA:** `4efcf062ea47cab19a92051532cfaa62d9692d14`
- **Final SHA:** `674631b` (implementation; `rejectedInsightIds` field added to the request in `7647799` alongside S02)
- **Branch:** `lane/atlas-snapshots-reflection`
- **Executor:** Claude Opus 5.5 (Atlas Snapshots + Reflection privacy lane)
- **Reviewer:** pending (integration owner)
- **Risk / proof class:** H snapshot provenance/privacy

## Changed-file envelope
- `src/atlas/eligibility.ts` — new: `selectQualifyingSnapshotProvenance`, `evaluateSnapshotEligibility`, `SNAPSHOT_ELIGIBILITY_RULES`
- `tests/atlas/eligibility.test.ts` — new, 11 tests
- `tests/atlas/fixtures.ts` — new synthetic fixture helper (shared with S02/RF09 tests)
- `docs/v2/proof/S01.md` — this receipt

No hot-zone file, persisted schema, migration, GameEvent, Worker, dependency or `main` change.

## Acceptance matrix
| Criterion | Evidence | State |
|---|---|---|
| Pure, deterministic function | no state writes; determinism test | PASS |
| Plan 15.3: first via milestone (≥6 distinct dimensions from qualifying evidence) or explicit request; later via bounded cadence (7 d) or explicit request (≥24 h); no CAMPAIGN_COMPLETED | milestone/cadence/too-soon tests | PASS |
| Counts only active, non-PRIVATE, non-retracted, player-stated evidence; confirmed insights / contradictions only when all support qualifies | model-proposed and mixed-insight tests | PASS |
| Never eligible from AdventureObservations alone | observations-only test; IDs absent | PASS |
| Typed request listing eligible provenance IDs (sorted) + `newSourceIds` vs previous Snapshot | first-milestone and cadence tests | PASS |
| Canary: PRIVATE/retracted-only material → not eligible, IDs absent, all triggers | canary test | PASS |
| Ineligible results carry no provenance IDs | canary + reasons-only shape | PASS |

## Validation executed
- Focused: `tests/atlas/eligibility.test.ts` 11/11
- `npx tsc --noEmit -p .`: clean
- Full unit `npx vitest run`: **532/532 across 72 files** (506 baseline + 26 lane tests). One earlier run had a single timing failure in `tests/cartographer/context.test.ts` ("600 turns" ceiling) under load; it passed in isolation (12/12) and on the full re-run. Untouched by this lane.

## Validation not executed
- Browser suite, `npm run build`, CI: not run locally by this lane.
- No UI wiring: the request path is not yet called from App/engine (hot zone).

## Privacy / cost / persistence impact
Reads provenance only via M06 `createV2ProvenanceVisibility`; never inspects prose. No persisted schema. No provider. No spend.

## Design notes for review
- Thresholds (`minEvidence 3`, `milestoneMinDimensions 6`, 7 d / 24 h) are proposal constants.
- Reflections are not counted: `atlasSnapshotSchema` has no `reflectionIds` field. Counting confirmed Reflections as Snapshot provenance would require adding `reflectionIds: string[]` to the persisted AtlasSnapshot schema — not done.
- A later Snapshot also requires ≥1 qualifying ID not referenced by the previous Snapshot (`no-new-material`).

## Verdict
READY_FOR_REVIEW

## Next packets
S02 (consumes request), S04 UI wiring.
