# F07 — Protected capability invariant manifest

## Packet

- packet: `F07`
- branch: `docs/v2-capability-invariants`
- foundation base: integration after V01 + M00
- output: `docs/v2/CAPABILITY_INVARIANTS.md`

## Method

Compiled only from:

- explicit controlling v2 product laws;
- current Operational State verified behavior;
- named repository tests and real-browser journeys;
- V01 STT-only CI evidence;
- M00 canonical v1 migration fixtures.

No source-code presence alone was promoted to a verified capability.

## Result

The manifest separates:

1. **VERIFIED** capabilities that future changes must preserve;
2. **REQUESTED** v2 capabilities that are controlling requirements but not yet implemented/proven.

It defines impact-radius validation subsets for:

- App decomposition;
- schema/persistence;
- provider/mode work;
- Adventure/Combat.

## Critical protections frozen

- deterministic game authority;
- structural PRIVATE filtering;
- provenance-safe retraction;
- permanent agency controls;
- reload/export/import;
- Worldwalker interaction;
- Boss/Mystery deterministic authority;
- async/double-submit safety;
- provider failure and zero-cost boundaries;
- STT editable-input lifecycle;
- no-TTS source/bundle rule;
- typing fallback;
- offline/PWA behavior;
- onboarding/hydration continuity;
- Greyson/Aerron asset identity;
- synthesis privacy;
- synthetic-only development data.

## Future invariants deliberately not promoted

Journal, Reflection, Adventure, Combat, Snapshot and v1->v2 migration behaviors are marked **REQUESTED** until their actual packets produce appropriate evidence.

## Verdict

**PASS.**

F07 supplies the proof obligations required before higher-risk interface and authority work such as D06.
