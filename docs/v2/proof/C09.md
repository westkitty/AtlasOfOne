# Proof Receipt — C09

## Identity
- **Packet:** C09
- **Title:** Deterministic enemy intent / telegraph state
- **Base SHA:** `4efcf062ea47cab19a92051532cfaa62d9692d14`
- **Final implementation SHA:** `7b24ba8`
- **Branch:** `v2/combat-c07-c12`
- **Pull request:** draft PR for this branch
- **Executor:** Claude Opus 5.5 (combat lane)
- **Reviewer:** pending
- **Risk / proof class:** H (intent tests)

## Changed-file envelope
- `src/combat/types.ts` (lane-owned: `CombatIntentPlan`, optional `CombatState.telegraphedIntents`)
- `src/combat/intent.ts` (new: `selectEnemyIntent`, `telegraphIntents`, `resolveEnemyPhase`)
- `tests/combat/intent.test.ts` (intent tests + full-loop objective sims)

No hot-zone file touched.

## Acceptance matrix
| Criterion | Evidence | State |
|---|---|---|
| no randomness; intent is a pure function of state | "pure function of state"; no Math.random/Date in source | PASS |
| intent visible before resolution | `resolveEnemyPhase` refuses a missing or stale telegraph | PASS |
| only telegraphed intents execute; forged out-of-envelope damage rejected | resolve + "rejects forged" | PASS |
| GUARD integrates the C03 formula (12 -> 6, timed -> 3) | GUARD test | PASS |
| charge -> charged release (22) unless interrupted | charging tests | PASS |
| protect: objective-action targets the ally; `protected-target` prevents it | protect test | PASS |
| enemy phase cannot be replayed | replay test | PASS |
| every MVP objective resolves in 2-5 turns under real enemy pressure | defeat 3, shielded+counter defeat 5, survive 3, protect 3 (uncovered -> defeat), interrupt 2, pacify 2 | PASS |

## Conservative interpretations (review)
- Selection priority: staggered -> recover; charging -> charged release; charging gimmick on odd rounds -> charge; protect -> objective-action on the ally; swarm -> scaled attack; otherwise attack 12. All damage stays inside the C00 10-22 envelope.
- If a telegraphed charged release was interrupted, it resolves as `interrupted` (0 damage) instead of re-telegraphing mid-phase. The change comes only from engine statuses, never from narration.
- Order: resolve intents in definition order -> tick statuses -> END_ENEMY_PHASE -> objective check -> telegraph next round.
- New constants for C17: ENEMY_ATTACK_DAMAGE 12, CHARGED_RELEASE_DAMAGE 22, OBJECTIVE_ACTION_DAMAGE 14.

## Validation executed (local macOS, head `4bde84e` before receipts)
- `npx tsc --noEmit -p .`: clean
- `npx vitest run`: **642/642 across 74 files** (baseline 506/506 across 69)
- Focused: `tests/combat/intent.test.ts` 18/18

## Validation not executed
- Build, browser suite and CI were not run by the executor (UNVERIFIED until PR CI). Intent display is C13.

## Privacy / cost / persistence impact
Personal-data eligibility: none. Provider context: none. `telegraphedIntents` is an optional in-memory field; C11 decides whether it becomes durable. Migration: none. Dependencies: none. Spend: none.

## Reviewer verdict
Executor: READY_FOR_REVIEW (not self-certified; H-class).

## Next packets unblocked
C11 (persist telegraphs), C13 (intent display), C17

## Integration review + repair (Claude Opus 5.5, integration owner)
Independent adversarial review (feature-dev:code-reviewer, read-only against PR head): **MERGE_READY**.

Combined integration after repair: 757/757 unit across 88 files, tsc clean.
