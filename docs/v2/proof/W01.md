# Proof Receipt — W01

## Identity
- **Packet:** W01
- **Title:** Adventure/world marker taxonomy and icon contract
- **Base SHA:** `45b57215db4a2dd0e1b9d6da16716d2c82e40aa9`
- **Branch:** `lane/v2-world-w01-w04-w09`
- **Executor:** Claude Opus 5.5 (world lane)
- **Risk / proof class:** S

## Changed-file envelope
- `src/world/markers.ts`
- `tests/world/markers.test.ts`
- `docs/v2/proof/W01.md` (this receipt)

No hot-zone, persistence schema, migration, Worker, dependency, or `main` mutation.

## Acceptance
- Typed `WORLD_MARKER_KINDS` covers every 11.2 category (adventure, journal shrine, NPC conversation, hostile/peaceful encounter, Mystery Door, Boss arena, memory, discovery, sanctuary, pure-fun): PASS
- Fixed glyph + label per kind in a frozen table: PASS
- No kind label contains psychological/analytical vocabulary (11.3 guard `containsPsychologicalLabel`): PASS
- `renderWorldMarker` rejects unknown kinds and place labels such as "This tests loyalty": PASS
- All current `REGIONS` labels pass the guard: PASS
- Visual render in the world canvas: UNVERIFIED (not wired; see wiring)

## Validation executed
- Focused: `tests/world/markers.test.ts` 10/10 (shared with W09)
- Lane head (W01+W02+W03+W04+W09 on `45b5721`), local macOS:
  - `npx tsc --noEmit -p .`: clean
  - `npx vitest run`: **787/787 across 92 files** (757 baseline + 30 new world-contract tests)
- Browser suite and `npm run build`: not run locally for this lane (no UI wiring changed).

## Validation not executed
- Visual/render proof in OverworldCanvas: not wired in this lane.
- Physical mobile / Safari / live provider: not applicable.

## Privacy / cost / persistence impact
Personal-data eligibility: none (synthetic fixtures only). Provider context: none. Persisted schema: none. Migration: none. Remote service/dependency: none. Spend: none. No hot-zone file touched; `src/App.tsx` untouched.

## Integration wiring required (integration owner; not done here)
- `src/App.tsx` / `WorldMap.tsx`: compute `placeAdventureMarkers(state.adventureSeeds, REGIONS.map(r => ({ territoryId: r.id, label: r.label, centre: r.centre })), (s) => adventureSeedIsEligible(state, s))` and render each via `renderWorldMarker(marker.kind, regionFor(marker.territoryId).label)`, showing `glyph` + visible `label` with `aria-label={accessibleName}` (never colour alone).
- `useWorldInteraction.ts`: each movement tick call `detectEncounterContact(playerPos, encounters)`; on `start-combat` transition the entity to `engaged` and start combat from `combatDefinitionId` via the combat engine; on `offer-interaction` show a prompt only. After combat resolves, transition to `resolved`.
- NPC records: load authored `NpcIdentity[]` into `createNpcRegistry`; resolve sprites by `artAssetId` through the art manifest; call `recordNpcAppearance` when a run features an NPC. Persisting `NpcState` requires a schema-v2 change (hot zone).

## Reviewer verdict
Executor self-assessment: contract-level PASS; awaiting integration-owner review.

## Next packets unblocked
W02, W09, W03
