# Proof Receipt — C12

## Identity
- **Packet:** C12
- **Title:** Combat authority firewall
- **Base SHA:** `4efcf062ea47cab19a92051532cfaa62d9692d14`
- **Final implementation SHA:** `4bde84e`
- **Branch:** `v2/combat-c07-c12`
- **Pull request:** draft PR for this branch
- **Executor:** Claude Opus 5.5 (combat lane)
- **Reviewer:** pending
- **Risk / proof class:** H/C (mutation proof)

## Changed-file envelope
- `src/combat/firewall.ts` (new: `validateCombatProposal`, `applyCombatNarration`; read-only import of `proposalContainsForbiddenAuthority` from `src/cartographer/proposals.ts`)
- `tests/combat/firewall.test.ts`

`src/cartographer/*` unchanged. No hot-zone file touched.

## Acceptance matrix
| Criterion | Evidence | State |
|---|---|---|
| only a strict `combat-narration` envelope with bounded string fields is accepted | accept test; unknown/wrong-kind/non-string/oversized rejected | PASS |
| model-authored HP, damage, outcome, reward, XP, objective progress, status, turn order, phase, round, intent and charges rejected, including nested | 27 adversarial `it.each` cases | PASS |
| mutation proof: for every proposal (valid and adversarial) state stays identical by reference and by JSON value; input is deep-frozen | 28 mutation cases | PASS |
| forged fields merged into ATTACK/ACT/TECHNIQUE commands and enemy-phase input do not change results | command-injection tests | PASS |
| no engine function mutates a deep-frozen state | final test | PASS |
| prototype-pollution JSON rejected | `__proto__` test | PASS |

## Conservative interpretations (review)
- No path leads from a proposal into any reducer. `applyCombatNarration` returns the input state by reference, plus a verdict. Wiring narration into the display is C13 scope.
- Combat-specific forbidden keys (phase, round, combatants, telegraphedIntents, progress, completed, victory, etc.) are checked on top of the shared cartographer list.

## Validation executed (local macOS, head `4bde84e`)
- `npx tsc --noEmit -p .`: clean
- `npx vitest run`: **642/642 across 74 files** (baseline 506/506 across 69)
- Focused: `tests/combat/firewall.test.ts` 66/66

## Validation not executed
- Build, browser suite and CI were not run by the executor (UNVERIFIED until PR CI). No live provider was exercised (by design).

## Privacy / cost / persistence impact
Personal-data eligibility: none. Provider context: none. Persisted schema: none. Dependencies: none. Spend: none.

## Reviewer verdict
Executor: READY_FOR_REVIEW (not self-certified; H/C-class).

## Next packets unblocked
C13 narration wiring, C15/C16 provider fixtures

## Integration review + repair (Claude Opus 5.5, integration owner)
Independent adversarial review (feature-dev:code-reviewer, read-only against PR head): **MERGE_READY**.

Combined integration after repair: 757/757 unit across 88 files, tsc clean.
