# M00 — Canonical synthetic schema-v1 export fixtures

## Packet

- packet: `M00`
- implementation branch: `test/v2-v1-canonical-fixtures`
- implementation head reviewed: `534b38a143c9b3a8ce0dbe8654075e5acf9ad0d6`
- pull request: #14
- purpose: freeze migration inputs before any schema-v2 mutation

## Changed files

- `tests/fixtures/v1/canonical-current-v1.json`
- `tests/fixtures/v1/canonical-legacy-v1.json`
- `tests/persistence/v1-canonical-fixtures.test.ts`

No production source, schema, migration, package manifest, Worker code, runtime asset, or dependency changed.

## Fixture contract

### Current v1 fixture

The current fixture is synthetic-only and preserves stable examples of:

- player/settings state;
- turns and evidence provenance;
- Insights and contradictions;
- map fragments;
- Boss run state;
- Mystery Door run state;
- private-topic state;
- presentation/history state;
- WorldJourney state;
- historical FinalAssessment state;
- onboarding state.

### Legacy v1 fixture

The legacy fixture deliberately omits additive fields introduced later inside schema v1:

- Boss/Mystery run fields;
- WorldJourney;
- FinalAssessment;
- onboarding marker;
- evidence origin/provider metadata.

The existing v1 schema must fill only its already-defined defaults.

## Validation

GitHub Actions **Atlas validation #62**, run `35883663289`, job `107258183804`, head `534b38a143c9b3a8ce0dbe8654075e5acf9ad0d6`:

- unit suite: **39 files / 306 tests passed**
- focused canonical-v1 fixture suite: **2 tests passed**
- production/PWA build: PASS
- PWA precache: **120 entries (597.67 KiB)**
- real Chrome browser suite: **15 files / 143 tests passed**

The focused tests prove the current fixture round-trips through the existing schema-v1 transfer path without semantic loss and the legacy fixture receives the expected additive v1 defaults.

## Privacy

All fixture content is explicitly synthetic. No real Greyson journal text, transcript, personal evidence, or private campaign content is present.

## Verdict

**MERGE_READY** for the v2 integration branch.

This packet does not implement schema v2 or v1 -> v2 migration. It freezes the before-state those later packets must preserve.
