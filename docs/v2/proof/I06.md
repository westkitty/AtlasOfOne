# Proof Receipt — I06

## Identity
- **Packet:** I06 — explicit Reflection can create supported Evidence
- **Executor / Reviewer:** Claude Opus 5.5 (integration owner; hot zone: types, schema-v2, context, retirement)
- **Risk / proof class:** H/C provenance + privacy

## Design decision (integration owner)
RF02 produced an evidence *proposal* but `EvidenceRecord` could not carry Reflection provenance (RF09 flagged this). Added optional `EvidenceRecord.sourceReflectionIds` (v2 schema, `min(1)` when present). Enforced at two layers:
1. Base evidence visibility (`src/cartographer/context.ts`, used by provider context): Reflection-sourced evidence travels only while every authorizing Reflection is active, non-PRIVATE, confirmed/partial.
2. v2 retirement (`src/persistence/retirement.ts`): evidence eligibility also requires the Reflection's full source chain; `retireIneligibleV2DerivedState` durably sets such evidence to `retracted` when the chain breaks (journal PRIVATE, Reflection PRIVATE/retracted). Cycle guard fails closed.
Conversion (`src/slice/evidence.ts`): RF02 decides whether a claim exists; claim text is Greyson's own response (or a confirmed interpretation); dimension/territory come from the seed + curiosity gap structure; origin player-stated, basis explicit, strength 2 (confirm) / 1 (partial). **No XP**: legacy `EVIDENCE_ADDED` (which awards XP) is deliberately not used. Wired into the existing RF01 decision handler in `src/App.tsx`.

## Changed-file envelope
- `src/game/types.ts`, `src/persistence/schema-v2.ts`, `src/cartographer/context.ts`, `src/persistence/retirement.ts` (hot zone, integration owner)
- `src/slice/evidence.ts` (new), `src/App.tsx` (decision handler)
- `tests/slice/evidence.test.ts` (9), `tests/browser/vertical-slice.test.ts` (extended)

## Acceptance
- Confirm with own words -> player-stated Evidence with Reflection provenance, structural dimension/territory, no XP: PASS (unit + browser).
- Partial -> strength 1; reject/uncertain/private/empty response -> nothing: PASS.
- Idempotent per Reflection: PASS.
- Export/import round-trip; empty provenance array rejected: PASS.
- PRIVATE Reflection, retracted Reflection, PRIVATE source Journal entry -> evidence durably `retracted`, ineligible, and absent from compiled provider context (canary string check): PASS (3 cases).
- Base layer withholds even before durable retirement: PASS.
- Crafted provenance cycle fails closed without recursion: PASS.
- Existing suites unchanged and green (turn-sourced evidence unaffected): PASS.

## Validation executed
typecheck 0; unit 1020/1020 across 114 files; build PASS (120 entries, 711.31 KiB); browser 156/156 across 20 files.

## Validation not executed
- Journal-sourced Reflections are not converted (no structural dimension); intentionally returns unchanged.
- Insights built on Reflection-sourced evidence: none are created today; insight eligibility uses the base layer only (documented gap).

## Privacy / cost / persistence impact
Persisted schema: additive optional field. Privacy: strengthened (two-layer retirement). No spend.

## Reviewer verdict
**MERGE_READY** (integration owner). Independent review requested with I09.

## Next packets unblocked
I07, I08, S01 now has reachable player-stated evidence from play
