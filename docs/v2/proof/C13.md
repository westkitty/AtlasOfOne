# Proof Receipt — C13

## Identity
- **Packet:** C13
- **Title:** Mobile CombatPanel and objective/intent display
- **Base SHA:** `45b57215db4a2dd0e1b9d6da16716d2c82e40aa9`
- **Branch:** `lane/v2-combat-presentation-content`
- **Executor:** Claude Opus 5.5
- **Risk / proof class:** S

## Changed-file envelope
- `src/combat/ui/combatPanelView.ts` — pure `buildCombatPanelView`, `leaveBlockedReason`, `STATUS_COPY`
- `src/combat/ui/CombatPanel.tsx` — props-in (`view`) / intents-out (`onIntent: CombatPlayerIntent`)
- `src/styles.css` — appended delimited `/* ---- v2 CombatPanel (C13) ---- */` section only
- `tests/combat/combat-panel.test.ts`
- `docs/v2/proof/C13.md`

**Not wired into `src/App.tsx`** (hot zone). The integration owner mounts
`<CombatPanel view={buildCombatPanelView(def, state, leaveCtx)} onIntent={(i) => setState(playRound(def, state, i, leaveCtx))} />`.

## Acceptance
- Shows player/ally/enemy HP as text plus bar, objective line + progress text, telegraphed intents with glyph + words, statuses with a distinct glyph, label and explanation: PASS.
- Five verbs ATTACK/TECHNIQUE/GUARD/ACT/LEAVE always rendered in order; LEAVE always visible, disabled with a visible reason when after-turn or story gated: PASS.
- Every disabled verb carries a reason; everything is disabled outside the player phase: PASS.
- Panel imports no engine mutators (source check); view builder is pure (no state mutation): PASS.
- Rendered via `react-dom/server` static markup in the node suite (no new deps, no DOM test setup exists): PASS.
- CSS: 44px min touch targets; no fixed px width > 44 in the section; 2-column verb grid with `minmax(0,1fr)` at narrow widths: PASS (static check).
- Non-colour status distinction: unique glyphs and labels per status: PASS.
- No horizontal overflow at 320px in a real browser: UNVERIFIED (no harness page without App wiring). The static CSS checks are a proxy.

## Validation executed
- Focused: `tests/combat/combat-panel.test.ts` 15/15.
- Full: `npx vitest run` **876/876 across 93 files** (119 new in this lane, so 757 pre-existing; baseline not run separately: in this lane: 66 CT04, 21 C14, 12 C15/C16, 5 CT09, 15 C13).
- `npx tsc --noEmit -p .` exit 0. Note: `-p .` has `files: []`, so it type-checks nothing. `npx tsc --noEmit -p tsconfig.app.json` reports 10 errors, all in pre-existing test files outside this lane (adventure/atlas/cartographer/game/journal/knowledge/persistence) and none in the files this lane changed.

## Validation not executed
- Browser 320/390/430 layout proof, keyboard focus order, and screen reader pass. These need App wiring (I02 / W10 / Q10).

## Privacy / cost / persistence impact
None. No provider, schema, migration, dependency or spend.

## Verdict
**READY_FOR_REVIEW** (component + view-model; browser layout proof deferred).

## Next packets
I02 (App wiring), W10, Q10, AS15.
