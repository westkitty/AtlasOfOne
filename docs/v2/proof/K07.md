# Proof Receipt — K07

## Identity
- **Packet:** K07
- **Title:** Deterministic KnowledgeGap -> AdventureSeed request object
- **Base SHA:** `4efcf062ea47cab19a92051532cfaa62d9692d14`
- **Branch:** `v2/knowledge-adventure-k07-a09`
- **Pull request:** draft to `integration/atlas-v2-journal-adventure-combat` (see PR body)
- **Executor:** Knowledge -> Adventure lane (Claude Opus 5.5)
- **Risk / proof class:** H knowledge selection / privacy boundary

## Changed-file envelope
- `src/knowledge/seedRequest.ts`
- `tests/knowledge/seedRequest.test.ts`
- `docs/v2/proof/K07.md` (this receipt)

No hot-zone file, `GameEvent` widening, persistence schema, migration, Worker, dependency, deployment, or `main` mutation.

## Acceptance
- K05/K06 eligibility runs BEFORE scoring/ranking; PRIVATE, retracted, retired, seeded and resolved gaps are never selected: PASS
- Privacy canary: PRIVATE-only and retracted-only gap states return null (no request): PASS
- A private gap that would outrank is withheld; the request exposes excluded IDs only, never summaries: PASS
- K01 base score recomputed from territory state; stored priority and summary prose ignored (dramatic wording cannot win): PASS
- K03 structural relevance (+10 open contradiction overlap) composed into ranked priority: PASS
- K04 exact-repeat cooldown reranks a just-selected gap behind another: PASS
- Typed strict zod request schema with inline schema snapshot + key-set assertion: PASS
- pure-fun kind rejected on the gap path (reserved for A09); caller may pick other registry kinds: PASS
- Deterministic and non-mutating: PASS

Note: request `learningTarget` is fixed to `reflection-eligible`; the model may propose only a premise downstream. Kind defaults to `investigation`; kind-selection policy beyond caller choice is not decided here.

## Validation executed (local macOS, branch `v2/knowledge-adventure-k07-a09`)
- `tests/knowledge/seedRequest.test.ts`: 11/11
- `npx tsc --noEmit -p .`: clean (exit 0)
- `npx vitest run` full unit suite: **554/554 across 75 files** (baseline 506/69 at `4efcf06` + 48 new tests in 6 new files; exact sum)
- First full run showed one timing failure in `tests/cartographer/context.test.ts` ("stays under an absolute ceiling even at 600 turns"), outside this lane; it passed 12/12 in isolation and the immediate full re-run was 554/554. Recorded as a load-sensitive flake, not caused by this change.

## Validation not executed
- `npm run build`, PWA precache count, browser suite: not run locally; left to PR CI.
- Physical device / live provider: not applicable (no provider path).

## Privacy / cost / persistence impact
Provider context: none. Network: none. Dependency: none. Spend: none. Persisted schema: none (no edit to `src/persistence/*`); any new durable fields or event wiring are deferred to the integration owner (A08). Fixtures are synthetic.

## Reviewer verdict
**REVIEW_REQUIRED** (executor self-check PASS; semantic review by integration owner pending on the draft PR).

## Next packets unblocked
A00 (consumes the request).
