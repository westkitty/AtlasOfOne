# Proof Receipt — M07

## Identity

- **Packet:** M07
- **Title:** Persistence torture across schema v1 and v2
- **Base SHA:** `47060a0d0ddb348b589bef306e64722275227994`
- **Branch:** `test/v2-persistence-torture`
- **Executor:** GPT-5.6 Sol / GitHub connector
- **Reviewer:** persistence-scope review + GitHub Actions #138
- **Risk / proof class:** R3 persistence/import/export regression proof

## Changed-file envelope

Tests only:

- `tests/persistence/v2-persistence-torture.test.ts`
- `tests/browser/v2-persistence-torture.test.ts`
- `docs/v2/proof/M07.md`

No runtime, schema, migration implementation, provider, UI production code, dependency, asset, deployment or `main` mutation.

## Proof chain

### Unit chain

1. canonical schema-v1 current fixture -> deterministic schema-v2 migration;
2. legacy FinalAssessment -> historical AtlasSnapshot;
3. add synthetic PRIVATE Journal -> gap -> seed -> memory provenance chain;
4. M06 retirement retires exclusive derived authority while preserving history;
5. save to IndexedDB;
6. reload;
7. export/deserialize;
8. delete;
9. restore;
10. exact state equality.

A second fixture proves v1 with no FinalAssessment remains snapshot-empty and stable through delete/re-import.

### Real-browser chain

1. import canonical schema-v1 fixture through the real Me-screen file control;
2. observe persisted schema v2 and deterministic historical Snapshot;
3. preserve turns, Boss/Mystery runs and worldJourney;
4. export through the real control;
5. delete local Atlas through the real confirmation flow;
6. re-import the exact exported schema-v2 file;
7. compare durable state;
8. reload and prove Snapshot history persists.

## Acceptance matrix

| Criterion | Evidence | State |
|---|---|---|
| real schema-v1 import migrates to v2 | browser fixture | PASS |
| historical Snapshot survives | unit + browser | PASS |
| v2 privacy retirement survives persistence | unit torture | PASS |
| IndexedDB save/reload exact | unit torture | PASS |
| export/import exact | unit + browser | PASS |
| delete removes old durable state | unit + browser | PASS |
| legacy no-assessment v1 remains snapshot-empty | unit torture | PASS |
| no production behavior changed by packet | test-only diff | PASS |

## Validation executed

GitHub Actions **Atlas validation #138**, run `35941268827`, implementation head `2924c662379c7e54d131317f96db92fa5d212e50`:

- `tests/persistence/v2-persistence-torture.test.ts`: **2/2 passed**
- unit: **49 files / 347 tests passed**
- production/PWA build: PASS
- PWA precache: **120 entries (597.60 KiB)**
- browser: **16 files / 138 tests passed**
- `tests/browser/v2-persistence-torture.test.ts`: **2/2 passed**
- full v1 -> v2 import -> persistence -> export -> delete -> re-import -> reload chain: PASS

## Validation not executed

- physical mobile/Safari/iOS
- physical mobile/Safari/iOS
- production deployment

## Privacy / cost / persistence impact

Tests use synthetic fixtures/canaries only. No real Greyson data, provider call, new storage path or spend path exists.

## Reviewer verdict

**MERGE_READY.**
