# Proof Receipt — N05

## Identity
- **Packet:** N05
- **Title:** Recurring NPC/place references a prior synthetic adventure
- **Base SHA:** `75ed3a5` (`origin/integration/atlas-v2-journal-adventure-combat`)
- **Branch:** `v2/world-consequences-memory`
- **Pull request:** draft to `integration/atlas-v2-journal-adventure-combat` (see PR body)
- **Executor:** World consequences + recurring memory lane (Claude Opus 5.5)
- **Risk / proof class:** S / unit proof (browser journey pending UI wiring)

## Changed-file envelope
- `src/adventure/memory/recurring.ts` (new)
- `tests/adventure/memory/recurring.test.ts` (new)
- `docs/v2/proof/N05.md` (this receipt)

## Contract
`selectRecurringReferences(state, { territoryId, currentRunId?, currentSeedId?, budget? })` -> `{ memories: RankedMemory[], fallbackLine: string | null }`.
Pipeline: `eligibleAdventureMemories` (N04 gate) -> territory trigger-term filter, excluding cards sourced from the current run/seed -> `retrieveAdventureMemories` (N02) -> character/place first (stable) -> `selectMemoriesWithinBudget` (N03).
`fallbackLine` names the first selected character/place only by its structural label (authored template title resolved from a trigger term), e.g. `A familiar face from "The Great Hat Heist" is somewhere nearby.`

## Acceptance
- Fixture: two slice runs in `identity`; second run's selection references run 1 and the line contains run 1's template title: PASS
- Journal canary and typed-action canary absent from selection: PASS
- PRIVATE-sourced memory (seeding journal privatized) never recurs; public pure-fun sibling still does: PASS
- A run never recurs into itself; other territories do not leak: PASS
- N03 budget honoured; empty territory rejected; empty selection -> null line: PASS

## Validation executed (local macOS)
- Focused: 5/5
- `npm run typecheck`: clean
- `npx vitest run`: **1036/1036 across 116 files** (shared with A07)

## Validation not executed
- Browser journey (plan's proof class): UNVERIFIED — scene/App wiring is out of lane (no App.tsx).

## Privacy / cost / persistence impact
Provider: none. Network: none. Persistence: none (read-only selector).

## Reviewer verdict
**MERGE_READY (after integration review)**; browser proof deferred to integration owner.

## Next packets unblocked
Integration wiring of the fallback line into scene rendering.

## Integration
Slice now calls derive/apply at completion and withdrawal (world memory accrues); outcome lines have a single source (derive.ts, imported by the slice). One A07 test fixture adjusted to start without cards because the slice already applies them (intent unchanged). Recurrence proven through the slice in tests/slice/loop.test.ts, incl. PRIVATE-source non-recurrence; AdventurePanel shows the N05 line on the hook beat.
