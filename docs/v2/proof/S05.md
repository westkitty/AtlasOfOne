# Proof Receipt — S05

## Identity
- **Packet:** S05
- **Title:** `what changed` comparison from supported deltas
- **Base SHA:** `45b57215db4a2dd0e1b9d6da16716d2c82e40aa9`
- **Branch:** `lane/reflection-atlas-rf04-rf08-s05`
- **Executor:** Reflection + Atlas lane (Claude Opus 5.5)
- **Risk / proof class:** H (synthetic comparison fixture)

## Changed-file envelope
- `src/atlas/compare.ts`
- `tests/atlas/compare.test.ts`
- `docs/v2/proof/S05.md` (this receipt)

No persisted schema, migration, GameEvent or hot-zone change.

## Acceptance
- `compareAtlasSnapshots(state, fromId, toId)` returns added/removed evidence, contradiction and insight IDs from provenance sets only (no prose diff): PASS.
- `insightIds` treated as provenance: each insight classified by LIVE status into `supported` / `rejected` / `pending`; rejected never appears as supported, including when rejected after the Snapshot: PASS.
- IDs whose sources are PRIVATE/retracted (M06 visibility) or missing are omitted and counted in `withheldCount` only: PASS.
- Historical Snapshots not mutated; reversed order, self-comparison and unknown IDs throw: PASS.

## Schema field request
`AtlasSnapshot.insightIds` still mixes supporting and rejected-interpretation IDs (see doc comment in `src/atlas/schema.ts`). S05 does not need a schema change, but a persisted `rejectedInsightIds` field (persistence owner) is requested so a Snapshot can record status *at creation time*; today only live status is available, so an insight confirmed at Snapshot time and rejected later is reported as rejected.

## Known limitation
Status flips on an insight present in BOTH Snapshots are not a set delta and are not reported. Surfacing them needs the field above (or a status-history record).

## Validation executed
- Focused: `tests/atlas/compare.test.ts` 7/7.

## Validation not executed
- UI presentation of the comparison: integration-owned.

## Privacy / cost / persistence impact
Reads existing state through M06 visibility. Persisted schema: none. Provider: none. Spend: none.

## Verdict
READY_FOR_REVIEW.

## Next packets unblocked
Snapshot comparison UI / Reflection-from-comparison integration.
