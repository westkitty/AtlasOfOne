# Proof Receipt — RF04

## Identity
- **Packet:** RF04
- **Title:** Persist rejected interpretations as anti-repeat context
- **Base SHA:** `45b57215db4a2dd0e1b9d6da16716d2c82e40aa9`
- **Branch:** `lane/reflection-atlas-rf04-rf08-s05`
- **Executor:** Reflection + Atlas lane (Claude Opus 5.5)
- **Risk / proof class:** H (recurrence test)

## Changed-file envelope
- `src/reflection/antiRepeat.ts`
- `tests/reflection/antiRepeat.test.ts`
- `docs/v2/proof/RF04.md` (this receipt)

No persisted schema, migration, GameEvent, Worker, dependency or hot-zone change. "Persist" is satisfied by existing persisted state (rejected ReflectionRecords and `rejected` InsightRecords); RF04 adds only the pure selector and guard.

## Acceptance
- `selectRejectedInterpretations(state)` returns rejected Reflection interpretations + live-`rejected` Insight titles, sorted by ID, deduped by normalized text: PASS.
- PRIVATE, retracted, or source-withheld (private journal / retracted evidence) rejections excluded entirely (no text, ID or count): PASS.
- `proposalReassertsRejected(proposal, rejected)` flags Reflection `interpretationCandidate` and Snapshot summary/claims on normalized exact or near-exact (edit distance <= min(3, 10% of length); < 10 chars exact only) match: PASS.
- Genuinely different text, question-only proposals, and retracted rejections are not flagged: PASS.
- No ML, synonyms or paraphrase detection: PASS (by construction).

## Validation executed
- Focused: `tests/reflection/antiRepeat.test.ts` 14/14.
- Full-lane counts: see PR body / final lane report.

## Validation not executed
- Wiring of the list into the provider context compiler and applying the guard at the P02/P04 parse boundary are integration-owned (I05).
- Paraphrase recurrence is intentionally out of scope.

## Privacy / cost / persistence impact
Provider context: produces a bounded list for callers; PRIVATE/retracted excluded. Persisted schema: none. Migration: none. Spend: none.

## Verdict
READY_FOR_REVIEW.

## Next packets unblocked
I05 (rejected first interpretation suppresses recurrence).
