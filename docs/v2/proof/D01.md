# D01 — Behavior-preserving presentation extraction

## Packet

- packet: `D01`
- implementation branch: `refactor/v2-presentational-extraction`
- implementation head reviewed: `c2deae3f3de9f333658e6f6b0a8fa48c2f021669`
- pull request: #16
- prerequisite: D00 extraction map merged into integration

## Objective

Reduce `src/App.tsx` collision surface by extracting presentation/navigation helpers without moving game, provider, persistence, STT, encounter or world authority.

## Changed files

- `src/App.tsx`
- `src/app/PresentationControls.tsx`

## Extraction

Added `src/app/PresentationControls.tsx` with pure prop/callback-driven presentation components:

- `ProgressDisplay`
- `AgencyControls`
- `AgencySheet`
- `PrimaryActionBar`

`App.tsx` still owns:

- all `GameEvent` construction and dispatch;
- provider/context/apply orchestration;
- persistence/import/delete;
- STT capture/transcription/cancellation lifecycle;
- Boss/Mystery deterministic mutation calls;
- Worldwalker movement/orchestration;
- onboarding;
- legacy assessment/snapshot handoff.

No state manager, context provider, dependency, service layer, schema, migration, provider contract, or engine authority was added.

## Scope review

The diff is exactly:

- `src/App.tsx` — markup replaced by component calls while callbacks remain App-owned;
- `src/app/PresentationControls.tsx` — new presentational component file.

No selectors/classes used by current browser tests were intentionally changed.

## Validation

GitHub Actions **Atlas validation #67**, run `35884595536`, implementation head `c2deae3f3de9f333658e6f6b0a8fa48c2f021669`:

- unit suite: **36 files / 284 tests passed**
- production Worker/client build: PASS
- PWA generation: **120 precache entries (588.07 KiB)**
- real Chrome browser suite: **14 files / 130 tests passed**

The full browser suite is the required gate because `App.tsx` changed.

## Protected capability result

No observed regression to:

- permanent agency controls;
- serious/quiet presentation;
- STT-only input;
- async/concurrency guards;
- Boss/Mystery interaction;
- Worldwalker;
- onboarding/hydration;
- provider fallback;
- offline/PWA journey.

## Verdict

**MERGE_READY** for the v2 integration branch.

Post-merge integration CI remains required to prove this extraction together with the current integration lineage.
