# Proof Receipt — P12

## Identity
- **Packet:** P12
- **Title:** Provider call-count / cost guard: mechanics never require a narrative call
- **Base SHA:** `75ed3a54038bb45aca07b830b47cdc0741580492`
- **Branch:** `lane/provider-p05-p10-p12`
- **Executor:** provider lane (Claude Opus 5.5)
- **Risk / proof class:** H/C

## Changed-file envelope
- `src/cartographer/modes/providerPolicy.ts` — `NARRATIVE_PROVIDER_OPERATIONS`, `MECHANICS_OPERATIONS`, `NARRATIVE_FALLBACK`, `providerCallAllowed(operation, quota)`, `consumeProviderQuota`
- `tests/cartographer/provider-cost-guard.test.ts` (6 tests)

## Acceptance
- Only the four narrative proposal operations (journal acknowledgement, reflection wording, adventure scene, snapshot synthesis) may call a provider; each names its local fallback: PASS.
- `providerCallAllowed` returns false for every mechanics operation even with a 1,000,000-call quota, for unknown operations, when quota is exhausted or zero, and for malformed quota (negative, fractional, NaN): PASS.
- `consumeProviderQuota` never exceeds the limit: PASS.
- Narrative and mechanics sets are disjoint: PASS.
- With global `fetch` stubbed to throw and count, the slice runs end to end: `exploreJournalEntry` -> `startSliceAdventure` -> `makeSliceChoice` x N -> deterministic combat via `playSliceCombatRound` -> pending Reflection -> `decideReflection` -> `applyReflectionEvidence` -> `privatizeJournalEntry` + `retireReflectionEvidence`; also `withdrawSliceAdventure`. Fetch call count: **0**: PASS.

## Validation executed (local macOS)
- Focused: **6/6**
- `npm run typecheck`: clean
- `npx vitest run`: **1047/1047 across 117 files**

## Validation not executed
- Policy is not yet enforced at a real call site (the provider client/worker are outside lane ownership); Q06 wires it.
- The fetch spy proves no `fetch`; it does not cover other transports (none exist in these modules).

## Privacy / cost / persistence impact
Cost guard only; no persisted state (quota is caller-held). No dependency or spend.

## Reviewer notes
`providerPolicy.ts` sits under `src/cartographer/modes`, which is excluded from the Worker tsconfig (see P05). Move it to `src/cartographer/` if the Worker must import it.

## Verdict
READY_FOR_REVIEW

## Next packets unblocked
Q06
