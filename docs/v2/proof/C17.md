# Proof Receipt — C17

## Identity
- **Packet:** C17 — Combat tuning pass against the 2-5-turn target
- **Base SHA:** `89f6fba` (`origin/integration/atlas-v2-journal-adventure-combat`)
- **Branch:** `lane/c17-ct10-tuning`
- **Executor:** Claude Opus 5.5
- **Risk / proof class:** H/C, deterministic encounter metrics

## Changed-file envelope
- `src/combat/content/actBank.ts` — two new 2-step interrupt ACT templates (`close_shutters`, `muffle_clapper`)
- `src/combat/content/encounters.ts` — `interrupt_dream_bell` and `interrupt_lamp_eater` use the 2-step ACTs
- `src/combat/content/scriptedPlayer.ts` — `cautiousScriptedPlayer` (metrics-only)
- `tests/combat/tuning-metrics.test.ts` (new)
- No engine/rule/verb or numeric constant changes.

## Metrics (simple scripted player, before -> after)
- Interrupt: all 6 at 2 turns / 0 player damage -> 4 at 2 turns / 0, 2 at 4 turns / 22 damage (one charged release goes unanswered while the 2-step ACT completes).
- Bank-wide after: turns {2: 9, 3: 12, 4: 8, 5: 1}; 25/30 encounters cost player or ally HP; max player HP lost 45 (beetle_tide); protect costs ally HP only.
- Cautious player (guards on a telegraphed charged release unless its plan already neutralises it): wins all 30 within 8 turns, never loses more HP than simple.
- LEAVE-seeker: `always` escapes turn 1; `after-turn` refused round 1, escapes turn 2; `story-gated` refused without gate, remains winnable, and yields `story` on turn 1 when the gate is open.

## Acceptance
- Simple player resolves every encounter in 2-5 turns with a win: PASS.
- Some real pressure (>=24/30 cost HP; every objective has an HP-costing encounter; interrupts not uniformly free): PASS.
- Nothing unwinnable for simple or cautious: PASS.
- LEAVE matches flee rules for all 30: PASS.
- Objective variety (5 objectives, none >30%): PASS.

## Validation executed
- `tests/combat/tuning-metrics.test.ts` 39/39; `tests/combat` 332/332 (17 files).
- Full: `npx vitest run` 1160/1160 across 123 files; `npm run typecheck` clean.

## Validation not executed
- Browser playtest of tuned encounters (needs I02 wiring).

## Privacy / cost / persistence impact
Synthetic content only. No provider, schema, migration, dependency or spend.

## Verdict
**READY_FOR_REVIEW**.

## Next packets
I02 browser proof; further pressure on technique-based interrupts would need a rule change (objective required=1), deliberately not done here.
